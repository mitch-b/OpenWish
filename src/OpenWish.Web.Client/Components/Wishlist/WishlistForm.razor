
@if (Model is not null)
{
    <EditForm Model="@Model" OnValidSubmit="@HandleSubmit" FormName="WishlistForm">
        <DataAnnotationsValidator />
        <ValidationSummary />

        <div class="mb-3">
            <label for="name" class="form-label">Name</label>
            <InputText id="name" class="form-control" 
            @bind-Value="@Model!.Name" />
            <ValidationMessage For="@(() => Model.Name)" />
        </div>

        <div class="mb-3">
            <label for="icon" class="form-label">Icon (emoji or single character)</label>
            <InputText id="icon" class="form-control" maxlength="1" placeholder="📋"
            @bind-Value="@Model!.Icon" />
            <div class="form-text">Choose an emoji or letter to represent your wishlist (optional)</div>
            <ValidationMessage For="@(() => Model.Icon)" />
        </div>

        <div class="mb-3 form-check">
            <InputCheckbox id="isCollaborative" class="form-check-input" @bind-Value="@Model!.IsCollaborative" />
            <label class="form-check-label" for="isCollaborative">
                Is Collaborative? (allows other invited members to add items to your list)
            </label>
            <ValidationMessage For="@(() => Model.IsCollaborative)" />
        </div>

        <fieldset class="mb-3">
            <legend class="form-label h6">Visibility</legend>
            <div class="form-check">
                <input class="form-check-input" type="radio" name="visibility" id="visibilityAllFriends"
                       checked="@(!Model.IsPrivate && !Model.IsFriendsOnly)"
                       @onchange="@(() => SetVisibility(false, false))" />
                <label class="form-check-label" for="visibilityAllFriends">
                    <i class="bi bi-people"></i> All Friends
                    <div class="form-text">All your friends can see this wishlist</div>
                </label>
            </div>
            <div class="form-check">
                <input class="form-check-input" type="radio" name="visibility" id="visibilitySpecificFriends"
                       checked="@(!Model.IsPrivate && Model.IsFriendsOnly)"
                       @onchange="@(() => SetVisibility(false, true))" />
                <label class="form-check-label" for="visibilitySpecificFriends">
                    <i class="bi bi-person-check"></i> Specific Friends Only
                    <div class="form-text">Only friends you choose can see this wishlist (manage in sharing settings)</div>
                </label>
            </div>
            <div class="form-check">
                <input class="form-check-input" type="radio" name="visibility" id="visibilityPrivate"
                       checked="@Model.IsPrivate"
                       @onchange="@(() => SetVisibility(true, false))" />
                <label class="form-check-label" for="visibilityPrivate">
                    <i class="bi bi-lock"></i> Private
                    <div class="form-text">Only you can see this wishlist</div>
                </label>
            </div>
        </fieldset>

        <div class="mt-4 d-flex gap-2">
            <button type="submit" class="btn btn-primary">@(Model.Id == 0 ? "Create" : "Update") Wishlist</button>
            @if (OnCancel.HasDelegate)
            {
                <button type="button" class="btn btn-outline-secondary" @onclick="HandleCancel">Cancel</button>
            }
        </div>
    </EditForm>
}
else
{
    <p>Loading form...</p>
}


@code {
    [Parameter] public WishlistModel? Model { get; set; }
    [Parameter] public EventCallback<WishlistModel> OnSubmit { get; set; }
    [Parameter] public EventCallback OnCancel { get; set; }

    private void SetVisibility(bool isPrivate, bool isFriendsOnly)
    {
        if (Model is not null)
        {
            Model.IsPrivate = isPrivate;
            Model.IsFriendsOnly = isFriendsOnly;
        }
    }

    private async Task HandleSubmit()
    {
        if (OnSubmit.HasDelegate)
        {
            await OnSubmit.InvokeAsync(Model);
        }
    }

    private async Task HandleCancel()
    {
        if (OnCancel.HasDelegate)
        {
            await OnCancel.InvokeAsync();
        }
    }
}